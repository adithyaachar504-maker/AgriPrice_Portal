== Python Soil Model ==

1. Install dependencies:
   pip install -r requirements.txt

2. Run the model server:
   python app.py

3. It runs on http://localhost:5000
   The Node.js server calls it automatically.

NOTE: Copy your dataset.csv and model.py here if not already present.
